# DEVMAN REVIEWS NOTIFACTION BOT

Данный бот уведомляет о новых ревью кода заданий с образовательной площадки [DEVMAN](https://dvmn.org/)

## Предустановка

У вас есть два способа по запуску бота - через виртуальное окружение или через создание docker-образа,
ниже описание по работе с каждым методом:

#### 1. Через виртуальное окружение.

Все используемые библиотеки указаны в файле **requirements.txt**

Для установки библиотек в виртуальное окружение используйте команды:

```
pip install -r requirements.txt
```

Создайте `.env` файл, в котором должны находиться ваши секретные данные:

```
DEVMAN_TOKEN=<YOUR_DEVMAN_TOKEN>
TELEGRAM_TOKEN=<YOUR_TELEGRAM_BOT_TOKEN>
TELEGRAM_CHAT_ID=<YOUR_CHAT_ID_ON_TELEGRAM>
```

#### 2. Через docker-образ.
Перед созданием образа обязательно заполните `.env` файл, в котором должны находиться ваши секретные данные:

```
DEVMAN_TOKEN=<YOUR_DEVMAN_TOKEN>
TELEGRAM_TOKEN=<YOUR_TELEGRAM_BOT_TOKEN>
TELEGRAM_CHAT_ID=<YOUR_CHAT_ID_ON_TELEGRAM>
```

В репозитории лежит файл `Dockerfile`, благодаря которому вы можете создать образ приложения и запустить в фоновом режиме.
Для этого вам понадобится установленный докер на ваш компьютер и следующие команды:

```
docker login -u <YOUR-USER-NAME>
docker build -t <IMAGE-NAME> .    |  Создание образа со всеми файлами из папки, где вместо IMAGE-NAME укажите название вашего образа
```

1. Чтобы узнать, как получить DEVMAN-токен - обратитесь к официальной документации [DEVMAN](https://dvmn.org/api/docs/).
2. Чтобы узнать получить токен телеграм-бота, вам его необходимо создать. Для этого найдите аккаунт `@BotFather` в телеграм и следуйте инструкции.
3. Чтобы узнать свой chat_id в телеграме напишите этому боту - `@userinfobot`.


### Начало работы

Для того, чтобы запустить бота - напишите в консоле:
```
python3 main.py
```

Или если вы используете докер:
```
docker run -dp 3000 <IMAGE-NAME>  |  Запускает ваш образ на порту 3000 в фоновом режиме.
```

Для остановки работы бота из консоли - нажмите комбинацию клавиш `ctrl+z`.

Если же вы захотите завершить процесс работы образа, используйте следующие команды:
```
docker ps  |  Позволяет увидеть все запущенные образы и их ID
docker stop <CONTAINER-ID>
docker rm <CONTAINER-ID>
```

## Деплой бота на хостинг Heroku

Вы можете захостить бота при помощи докер-образа или же GitHub репозитория.
Инструкции по каждому из способов:

#### Используя GitHub репозиторий
1. Зарегистрироваться на сайте [Heroku](https://heroku.com/) и создать свое приложение. Для удобства мной был выбран Европейский сервер.
2. Далее вам нужно будет создать репозиторий на [GitHub](https://github.com/), чтобы привязать его к Heroku. Вы можете клонировать данный репозиторий или сделать форк к себе.
3. Перейдите во вкладку **Settings** в панеле управления приложением на [Heroku](https://heroku.com/) и установите `Config Vars` в таком виде:

KEY | VALUE
------ | ------
DEVMAN_TOKEN   | YOUR_DEVMAN_TOKEN
TELEGRAM_CHAT_ID | YOUR_TELEGRAM_CHAT_ID
TELEGRAM_TOKEN | YOUR_TELEGRAM_BOT_TOKEN

4. Далее во вкладке **deploy** на сайте [Heroku](https://heroku.com/) найдите свой репозиторий по названию и нажмите кнопку **Deploy**.
5. Далее зайдите во вкладку **Resources** на [Heroku](https://heroku.com/) и запустите bot-процесс.

Если вы все сделали правильно бот должен написать вам сообщение вида:

```
🔥 Бот запущен!
```

#### Используя Docker-образ.
1. Зарегистрироваться на сайте [Heroku](https://heroku.com/) и установить [Heroku CLI](https://devcenter.heroku.com/articles/heroku-cli#download-and-install).
2. Находясь в директории с ботом используйте следующие команды в консоли:
```
heroku login | После чего нажмите любую кнопку и вас перекинет на сайт Heroku, где нужно пройти аутентификацию
heroku create <APP-NAME> | Создание вашего приложения
heroku container:push --app <APP-NAME> worker | Пуш вашего образа на Heroku
heroku container:release --app <APP-NAME> worker  |  Запуск вашего образа
```
3. Далее зайдите во вкладку Resources на [Heroku](https://heroku.com/) и запустите worker-процесс.
Если вы все сделали правильно бот должен написать вам сообщение вида:
```
🔥 Бот запущен!
```

Если этого не произошло - вы можете посмотреть логи с сервера, напишите в консоле:
```
heroku logs --app=<Название-вашего-приложения>
```

Для того, чтобы увидеть другие возможности логов, напишите в консоле:

```
heroku logs --help
```

## Создано при помощи

* [DEVMAN](https://dvmn.org/) - Обучающая платформа

## Авторы

* [Alexander Zharyuk](https://github.com/AlexanderZharyuk)
